name: Build and Push GTK Flatpak

on:
  push:
    branches:
      - 'rust-launcher'
      - '!master'
    paths:
      - 'moe.launcher.an-anime-game-launcher-gtk.yml'
      
  workflow_dispatch:

jobs:
  flatpak:
    name: "Flatpak"
    runs-on: ubuntu-22.04
    container:
      image: bilelmoussaoui/flatpak-github-actions:gnome-43
      options: --privileged
    steps:
    - uses: actions/checkout@v3
      with:
        ref: rust-launcher
        submodules: true
    - name: Install Deps
      run: |
        dnf install -y https://download-ib01.fedoraproject.org/pub/fedora/linux/development/37/Everything/x86_64/os/Packages/g/gtk4-devel-4.8.1-1.fc37.x86_64.rpm https://download-ib01.fedoraproject.org/pub/fedora/linux/development/37/Everything/x86_64/os/Packages/g/gtk4-4.8.1-1.fc37.x86_64.rpm https://download-ib01.fedoraproject.org/pub/fedora/linux/development/37/Everything/x86_64/os/Packages/l/libadwaita-1.2.0-1.fc37.x86_64.rpm  
    - uses: bilelmoussaoui/flatpak-github-actions/flatpak-builder@v4
      name: "Build"
      with:
        bundle: moe.launcher.an-anime-game-launcher-gtk.flatpak
        manifest-path: moe.launcher.an-anime-game-launcher-gtk.yml
        cache-key: flatpak-builder-${{ github.sha }}
    - uses: bilelmoussaoui/flatpak-github-actions/flat-manager@v3
      name: "Deploy"
      with:
        repository: stable
        flat-manager-url: ${{ secrets.REPO_URL }}
        token: ${{ secrets.REPO_TOKEN }}
